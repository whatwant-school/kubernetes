apiVersion: batch/v1
kind: Job

metadata:
  name: job-pi

spec:
  template:
    spec:
      containers:
      - name: pi
        image: perl:5.34.1
        command: ["perl",  "-Mbignum=bpi", "-wle", "print bpi(2000)"]

      restartPolicy: Never

  backoffLimit: 4
